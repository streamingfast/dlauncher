export const MAX_GRAPH_DRIFT = 15 * 60 * 60; // max drift value we display on the gaph
export const INFINITE_DRIFT_THRESHOLD = 9000000000;
export const DRIFT_RETENTION = 60; // retain half an hour of data
